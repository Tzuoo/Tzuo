# 網頁專案規則

## 修改原則

- 本檔位於 Git 根目錄下的 `網頁/AGENTS.md`；在儲存庫根目錄執行 Git，勿再建立根目錄規則或說明副本。
- 公開儲存庫只發布 `網頁` 與 `油猴腳本` 兩類資料；根目錄 `Tzuo.html` 僅保留舊網址轉址，禁止當成完整頁面編輯。
- `油猴腳本` 成品來源是相鄰私人專案的 `腳本成品`，不可在公開副本直接修改或上傳帳密、權杖與本機設定。
- 修改前完整閱讀 `Tzuo.html` 的 HTML、CSS 與 JavaScript 呼叫關係。
- 保留既有頁面功能、資料格式與操作方式，只修改本次需求範圍。
- 完成後檢查 HTML 內嵌 JavaScript 語法、瀏覽器主流程與 `git diff --check`。
- 未在實際瀏覽器完成互動驗證的項目標示「待實測」。
- 完成修改後更新本檔調整紀錄，同一問題不重複新增。

## 調整紀錄

### 2026-09-15：加入清單後重設總計倍率

- 影響檔案：`網頁/Tzuo.html`、本檔；每次成功加入清單後清空 `totalMultipliers`，讓現有各項倍率重新顯示並套用預設值 `1`。
- 重設發生在新項目加入後、保存及重新渲染前；清單項目與價格不清除，總和值立即按倍率 1 重新計算。
- 僅成功加入時重設；輸入驗證失敗、切換模式、確認下一欄與一般價格輸入不重設。
- 完成後驗證手機與外接鍵盤下的倍率重設、清單保留、總計重算及既有完整回歸；真實 iPhone Safari 待實測。

### 2026-09-15：恢復價格色彩與倍率整值覆寫

- 影響檔案：`網頁/Tzuo.html`、本檔；不改既有資料結構、計算與快捷鍵。
- 使用者影片確認手機橫排後各星價格輸入值皆呈現相同顏色；二／三／四星輸入文字及聚焦外框改與標籤共用藍／綠／黃，車／化雙同樣沿用各自顏色。
- 總計倍率在 focus 或 click 時標記下一次輸入為整值取代，並於 beforeinput 再次設定完整選取，避免 Safari 點擊後把游標放到預設值 `1` 前方；輸入後立即取消標記。
- 小數鍵的自訂處理及外接鍵盤 readonly 編輯同步遵守整值取代；點擊後輸入數字、小數或外接鍵盤皆不會與舊值串接。
- 驗證：headless Edge 確認三種星別顏色互異且與標籤一致，手機點擊後 `1`→`5`、小數整值取代及外接鍵盤整值取代通過；320／390／430px 排版、計算、捲動、手機與外接鍵盤全數回歸通過。iPhone Safari 實際選取顯示仍待實測。

### 2026-09-15：手機價格欄位橫排

- 影響檔案：`網頁/Tzuo.html`、本檔；只調整手機／觸控排版，不改價格資料、計算或桌面版。
- 各星價格改為每列三欄，二／三／四星同一列；更多星別依序每三欄換列。標籤置頂、輸入框置下並置中，避免窄欄水平溢出。
- 車／化雙價格改為每列兩欄。`carPriceEditor` 改回傳單一 price-row，統一由 car-price-grid 包裝，原事件及 disabled 邏輯保留。
- 完成後以 320／390／430px 手機型態確認價格排列、無水平溢出、小數輸入、計算及原手機／外接鍵盤／捲動回歸；真實 iPhone Safari 待實測。

### 2026-09-15：撤回固定摘要與模式按鈕

- 使用者實際查看後認為效果未改善，依要求撤回 `d7b94af` 的固定摘要及固定模式切換按鈕。
- `網頁/Tzuo.html` 完整回復至 `b9099c4`；保留手機輸入、倍率排列、捲動與外接鍵盤模式，不清除資料或設定。
- 以新提交撤回並推送，保留 Git 歷史，不強制覆寫遠端。語法、差異及手機輸入／外接鍵盤／倍率回歸檢查通過；iPhone 操作仍待實測。

### 2026-09-15：輸入時維持視窗位置

- 影響檔案：`網頁/Tzuo.html`、本檔；不修改計算、資料或快捷鍵設定。
- 使用者影片證實先前兩影格還原未擋住 Safari 延遲的價格欄位聚焦捲動，不可視為實機已修正。
- 改為固定頁面框架及 main 內部捲動區；只接受手動觸控滑動、滾輪、捲動按鍵／捲軸的新位置，聚焦與輸入更新會持續抵銷自動捲動，而非只等兩影格。工作區仍停用捲動錨定。
- 保留原欄位與同步全選，禁止重複延遲全選造成快速輸入時誤刪／覆寫。內容變短時允許捲動位置縮至合法上限。
- 驗證：內嵌 JavaScript 語法、差異檢查及原手機輸入／倍率排版回歸通過；headless Edge 模擬 300ms 延遲聚焦捲動被擋住，手動 wheel／touchmove 可正常捲動。
- 真實 iPhone Safari 軟鍵盤、工具列展開收合與輸入位置維持仍待實測。

### 2026-09-15：外接鍵盤模式

- 影響檔案：`網頁/Tzuo.html`、本檔；頂部加入手機／外接鍵盤模式按鈕並保存選擇。
- 外接模式將輸入框設為 readonly 及 inputmode none，不依賴 Safari 的虛擬鍵盤 API；價格／倍率／直接數值的實體數字、小數及刪除由程式處理，組數快捷鍵保留。
- 收到可信 Numpad 按鍵會自動啟用外接模式；無法保證插入瞬間辨識所有硬體鍵盤，首次可手動切換。拔除後需切回手機鍵盤模式，不可因誤判永久封鎖手機輸入。
- headless Edge 驗證模式保存、手機模式復原、Numpad 自動切換、價格小數及退格通過；真實 iPhone 外接鍵盤與手機鍵盤隱藏仍待實測。

### 2026-09-15：手機總計倍率單行排列

- 影響檔案：`網頁/Tzuo.html`、本檔；僅調整手機／觸控裝置倍率排版，不變更計算及資料。
- 原因：倍率輸入框固定寬度使二、三、四星換行，增加總計高度。
- 手機倍率區改為三欄等寬網格，輸入框允許縮小，二、三、四星同一行；更多星別仍按三欄換行。
- 驗證：內嵌 JavaScript 語法及 `git diff --check` 通過；headless Edge 在 320／390／430px 手機型態皆確認三個倍率同一行、無水平溢出，非均一小數倍率計算正確。真實手機 Safari 顯示仍待實測。

### 2026-09-15：手機組數輸入

- 影響檔案：`網頁/Tzuo.html`、本檔；外層舊網址轉址頁不變。
- 原因：組數區原為 `div`，只接收實體鍵盤事件，手機無法喚起輸入法。
- 在原顯示區加入可聚焦的 numeric 文字輸入欄位，以 input／composition 事件接收手機與全形數字；保留實體鍵盤快捷鍵，避免重複輸入。
- 輸入過程保留聚焦欄位的 DOM，不可每打一碼重建欄位；新增確認／下一欄及加入清單觸控按鈕。
- 驗證：內嵌 JavaScript 語法及 `git diff --check` 通過；headless Edge 的 390px 手機型態回歸通過（連碰、小數價格、加入清單、單／雙位立柱、退格、車上限、實體鍵數字與 Enter），無頁面錯誤。真實手機輸入法喚起、連續輸入及外接鍵盤硬體仍待實測。
- 遠端已確認為 `Tzuoo/Tzuo`；推送會更新 GitHub Pages，發布前須取得使用者明確同意。

### 2026-09-13：公開網頁與油猴成品發布

- 禁漫天堂成品同步至 5.2.2：收藏卡片擷取排除導覽／隨便看，隔離錯誤快取並保留完結分類；網站實測待確認。
- 禁漫天堂 5.3.0 加入逐本移除收藏確認、帳號核對、失敗保留及快取同步；模擬回歸通過，真實網站操作待實測。
- 四個網頁檔案集中至 `網頁`；根目錄保留舊 `Tzuo.html` 轉址並保留查詢參數及錨點。
- `油猴腳本` 僅放四支成品，私人專案維護工具自動同步發布；自動簽到及參考腳本不公開。
- 轉址／內嵌 JavaScript 語法、成品一致性與更新網址需檢查；油猴定時自動更新仍待 Edge 實測。
- 本次轉址參數保留、內嵌 JavaScript 語法與成品發布檢查均通過；頁面內容未修改。
- GitHub Pages 已建置完成；新頁面 HTTP 200 且內容一致，舊頁面 HTTP 200 且含轉址程式。實際瀏覽器轉址及互動仍待實測。

### 2026-08-27：拆分為獨立專案

- `Tzuo.html` 與網頁對話資料獨立歸入本資料夾。
- 本機專案改連既有公開儲存庫 `Tzuoo/Tzuo`，沿用原 GitHub Pages 網址與歷史；不再使用重複的私人 `Tzuoo/webpage`。
